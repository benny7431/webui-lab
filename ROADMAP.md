# 放首歌：功能地圖與課程取用規劃

建立日期：2026-09-21（Asia/Taipei）。本文件記錄已確認方向與建議路線，不代表功能已完成；即時進度與理解證據見 [PROGRESS.md](PROGRESS.md)，教材分析與實作流程見 [AGENTS.md](AGENTS.md)。

目前取得的最新教材是含 W01、W02、W03 的 [WEB03 整合版](https://moodle.ncnu.edu.tw/pluginfile.php/170589/mod_resource/content/0/web03.md)。W01 的建檔與初次提交順序、W02 的畫面與企畫、W03 的公開部署分工依其原文核對；本功能地圖不表示這些階段已完成。

## 已確認的方向

- 「放首歌」是獨立、從基礎建立的課程專案，位置為 `E:\AI\web程式設計\code`。原「聽風拾曲」只供唯讀參考與評估重用，不在修改範圍內。
- 完整目標以本次核對的 Noir 播放器體驗為基準，包含 Noir 外觀與操作、歌曲瀏覽／搜尋／分類／收藏／管理、完整播放控制、音訊特效、必要資料保存、編輯權限、模型設定、AI 自動整理，以及驗證與獨立上線。
- 依老師每次提供的教材取用功能，不預先指定每週必須完成哪一區。固定題目與繳交格式優先符合，再判斷能否整合到網站。
- 學生轉述老師要求期末提供完整專題；本專案以期末完整交付為目標，正式期限與評分細節仍待教材核對。階段成果可先展示，但不能據此刪除音訊特效或 AI 等最終目標，也不預設延到課後才完成。
- 平時深入學習課程核心；課程未涵蓋的進階功能可由 AI 協助整合。理解標準見下方「交付安排與理解深度」，不要求逐行推導所有進階演算法。
- 原播放器之後新增的功能另行討論。一般技術選擇由代理依教材、現有程式與部署條件提出理由和取捨；以下建議不構成提前建置完整架構的要求。

## 原專案參考基準與限制

- 原專案：`C:\Users\user\Documents\Codex\2026-07-31\new-chat`。
- 2026-09-21 唯讀核對時，Git 基點為 `d77b0dacc814982725d8102494a44f3a0dde61fd`；`app/`、`lib/`、`db/`、`tests/` 與 `package.json` 對此版本沒有差異。原專案另有文件、外部工具等未提交變更，全部保留。
- 下列為本機文件與原始碼證據；本次未啟動原站、重跑測試或驗證線上部署，不能據此宣稱所有功能已實測通過。之後參考檔案若改變，必要時唯讀對照上述 Git 版本，不改動原專案工作目錄。

| 已核對的原版行為 | 對放首歌的影響與來源 |
| --- | --- |
| `/test` 的 Noir 與 Paper 主頁共用 `Home` 與播放狀態；Noir 主要呈現播放與特效，歌庫管理多在 Paper，模型另有頁面。 | 需要整合管理入口；建議以 Noir 為主畫面、歌庫管理採可開合區，模型設定另有受保護入口。介面細節在相關小目標確認。[畫面分支](<C:/Users/user/Documents/Codex/2026-07-31/new-chat/app/page.tsx>)。 |
| 收藏存在共用歌庫，修改需要編輯權限；分類決定播放池，文字搜尋只縮小顯示清單。 | 先以這些原版行為作對照；個人帳戶、每人獨立收藏或其他播放池規則屬另議需求。[歌曲資料與請求](<C:/Users/user/Documents/Codex/2026-07-31/new-chat/app/songbook-http.ts>)。 |
| 直接音訊播放器提供分析資料；YouTube 嵌入播放沒有同樣的聲音資料。無可用分析或暫停時，光場回到目前配色的底色。 | G 需要可分析的音源；實際使用前核對音檔使用權限、格式及跨站存取設定。不能把能播放當成能分析。[音訊說明](<C:/Users/user/Documents/Codex/2026-07-31/new-chat/docs/context/audio.md>)、[播放來源分支](<C:/Users/user/Documents/Codex/2026-07-31/new-chat/app/player/playback-engine.ts>)。 |
| AI 工作保存於後端，由開著的頁面推進；手動標籤與排除優先於 AI。模型請求依賴 Responses、網路搜尋及結構化結果。 | 不能假設任意「相容」模型服務都能接上；需驗證選定服務。關閉所有頁面後仍持續整理屬額外背景執行需求。[整理機制](<C:/Users/user/Documents/Codex/2026-07-31/new-chat/docs/context/tagging.md>)、[模型請求](<C:/Users/user/Documents/Codex/2026-07-31/new-chat/lib/codex-song-tags.ts>)。 |
| 原版使用 React／TypeScript／vinext，歌單動態依賴 GSAP，光場使用原生 WebGL2；後端使用 Cloudflare D1 與 R2。 | 行為和部分獨立邏輯可參考；畫面元件與雲端服務不能假設直接搬入即能運作。音訊同步 CLI／Windows 工具另有依賴，不作為課程起步的必要前置。[系統說明](<C:/Users/user/Documents/Codex/2026-07-31/new-chat/docs/architecture.md>)。 |

## 功能地圖與課程對照

箭頭表示可拆的小成果，不是週次承諾。各區可依教材交錯進行，只補當次需要的前置；一個小目標可以跨多次對話。

| 分區 | 最終成果與可累積的小成果 | 課程概念 | 最少前置 |
| --- | --- | --- | --- |
| A．專案基礎 | 用途與開啟方式清楚、有可追溯紀錄；保留既有設定 → README → 基本 `index.html` → 首次提交與 GitHub → 持續更新操作及學習紀錄。 | Git、版本差異、文件、AI 協作紀錄。 | 現有環境即可，之後貫穿各區。 |
| B．頁面結構 | 在基本首頁上逐步加入歌曲資訊、歌單、播放控制與特效區域；一首靜態歌曲 → 清單 → 接上真正資料。 | HTML 語意、標題層級、清單、表單、按鈕、無障礙標示。 | A 的 Git 設定與基本首頁；不必等完整部署。 |
| C．Noir 外觀與互動 | 深色配色、字體層次、封面焦點、控制區與手機介面；配色 → 排版 → 響應式配置 → 焦點移動、拖曳與動態。 | CSS 選擇器、盒模型、繼承、Flexbox／Grid、媒體查詢、轉場；複雜動態屬延伸。 | B；播放連動需要 E。 |
| D．歌庫操作 | 瀏覽、搜尋、分類、收藏、新增、編輯與刪除；資料產生列表 → 搜尋 → 分類／收藏 → 表單管理 → 手動標籤及排除規則。 | 陣列、物件、函式、篩選、DOM 更新、事件、表單驗證。 | B 與最小歌曲資料；永久保存及正式權限接 F。 |
| E．播放器 | 播放／暫停、上下首、進度、音量、隨機、循環與狀態處理；單首音訊 → 自訂控制 → 播放順序 → 模式切換 → 失效音源與快速切歌。 | HTML 音訊、瀏覽器事件、狀態、非同步操作、資源生命週期。 | B、D 的基本歌單及一個可播放音源；不必等完整後端。 |
| F．資料與服務 | 歌曲、偏好及模型設定保存，音訊服務與編輯權限；偏好保存 → Fetch 讀 JSON → 歌庫 API → 伺服器保存／權限 → 音訊託管。 | 瀏覽器儲存、JSON、HTTP／HTTPS、REST、CORS、資料庫、身分驗證與授權；Cookie／Session／JWT 依教材取用。 | 偏好保存只需相關功能；歌庫 API 需要 D 的資料規則。音源驗證可提早進行。 |
| G．音訊特效 | 隨聲音變化的光場與配色；取得聲音強弱 → 驅動畫面參數 → 接上原版等級光場 → 手機效能、暫停、減少動態及不支援時的處理。 | Web Audio、Canvas、動畫更新、效能與清理；WebGL／著色器屬進階延伸。 | E 的穩定播放器與可分析音源；不必等待 H。 |
| H．AI 整理 | 受保護的模型設定、自動標籤、工作進度、暫停／恢復與失敗處理；單首整理 → 驗證結果 → 安全套用 → 多首工作與進度保存。 | 伺服器 API、非同步流程、JSON 驗證、權限與錯誤處理；工作排程屬延伸。 | D 的標籤規則與 F 的後端、保存、權限。 |
| I．完整交付 | 能獨立展示、操作和說明；每步檢查 → 跨功能驗證 → 手機、權限及失敗情境 → 操作文件 → 獨立部署與線上確認。 | DevTools、測試、除錯、環境設定、部署、HTTPS。 | 每區逐步驗證；完整交付需要全部目標達標。 |

## 重用判斷、成果驗證與理解確認

以下原版檔名均相對於上方原專案路徑。它們是參考位置，不是已搬入本專案的檔案。引用前讀取實際依賴與使用位置，必要時調整或改寫，說明原因及可保留成果。

| 分區 | 可參考或重用的部分與調整 | 成果驗證 | 具體理解確認範例 |
| --- | --- | --- | --- |
| A | 保留本專案設定；原 README 只參考組織方式，開啟及執行指令依本專案實況。 | 能照文件開啟、查看差異並完成實際提交。 | 說明暫存與 commit 的差別；已確認的署名設定範圍不重考。 |
| B | 參考原版資訊層次，React 標記改成符合當前階段的 HTML。 | 標題層級、按鈕、表單標示與鍵盤操作正確。 | 增加歌曲內容，選擇合適的 HTML 元素。 |
| C | 挑選 `app/test/motion.css` 的配色與尺寸；調整結構相依樣式。`motion-player.tsx` 綁定 React／GSAP，需評估改寫或局部移植。 | 桌機、窄螢幕、觸控與鍵盤可用，動態不妨礙操作。 | 調整間距或手機排列，從 DevTools 找出生效的樣式。 |
| D | 沿用 `app/page.tsx` 搜尋／分類規則、`lib/song-tags.ts` 標籤語意；事件處理與保存方式配合新專案。 | 搜尋、空結果、空白輸入、編輯與取消刪除符合預期。 | 把只搜尋歌名改成同時搜尋歌名和歌手。 |
| E | `app/playlist-navigation.ts` 與對應測試可取用；`app/player/` 的原生音訊處理可調整，React 包裝需改寫。 | 驗證第一／最後一首、空歌單、失效音源、快速切歌、隨機與循環，畫面與實際播放一致。 | 修改最後一首結束後停止或重頭播放的規則。 |
| F | 參考 `app/songbook-http.ts`、`app/api/`、`db/schema.ts` 的資料及權限分界；D1／R2 和原版金鑰處理需按新環境重新評估。 | 重載與跨瀏覽器保存符合設計；未授權修改被拒；保存失敗不顯示假成功。 | 在 Network 面板指出請求內容與狀態，說明為何隱藏按鈕不能代替伺服器權限。 |
| G | `app/test/` 的 `audio-reactive-analysis.ts`、`breath-driver.ts`、`field-palette.ts`、`field-renderer.ts`、`field-shaders.ts` 較獨立；型別、啟動與清理配合新環境。 | 同一首歌的強弱、暫停、換歌確實反映到畫面；隱藏分頁能停止繪圖，不支援特效時仍可播放。 | 說明聲音資料到畫面的流程，修改低音對亮度或大小的影響並預測結果。 |
| H | 參考 `lib/codex-song-tags.ts`、`lib/tagging-*.ts` 和 `app/use-tagging-queue.ts` 的規則；服務連線、機密與排程依新後端調整。 | 測試錯誤金鑰、逾時、格式錯誤、有限重試及分析途中修改歌曲；保留手動修改，舊結果不覆蓋新資料。 | 說明修改歌手後，先前分析結果為何應作廢或重新整理；指出 AI、原版與本次調整的部分。 |
| I | 原版 `tests/` 作為案例來源，改成對新實作有意義的檢查；補實際瀏覽器及新部署驗證。 | 全新瀏覽器能使用獨立公開網址；正常與相關失敗情境、手機、權限、聲音與文件皆核對。 | 依 Console／Network 的錯誤定位問題，能按操作說明展示作品。 |

## 建議技術路線與累積原則

- 先用 HTML、CSS、原生 JavaScript。只有教材要求或實際維護問題能說明效益時，才評估 React 或其他工具；後端與資料庫在相關階段依教材及部署限制決定。
- 第一份真實歌庫先保持穩定的 `id`、`title`、`artist`；依需要加入物件形式的 `tags`、`favorite`。播放階段區分 `sourceUrl`（來源頁面）與 `audioUrl`（可直接播放的音檔），AI 階段再補來源、排除與資料版本。不提前填滿未使用欄位，也不把 YouTube 的 `videoId` 當成所有歌曲的身分。
- 畫面負責顯示與接收操作；歌庫函式處理資料；播放器維持唯一播放來源與狀態；特效讀取播放器提供的分析資料；資料存取負責讀寫。先用清楚的函式區分，做到相關階段才拆必要檔案。
- 偏好可先用 `localStorage`；歌庫可由記憶體資料接到 JSON，再接正式 API。若作業要求本機歌曲保存可安排；改為共用歌庫後明確以伺服器資料為準。替換存取方式時保留搜尋、資料規則及可沿用的畫面。
- 先用少量允許展示及託管的音檔確認播放與分析。簡單亮度反應是 G 的中間成果，後續保留分析流程、接上完整光場；最終以原版視覺及互動作對照驗收。
- H 從單首請求、結構驗證及安全套用，擴充為完整自動整理。機密留在受保護的伺服器設定；呼叫額度、重試上限和錯誤提示依選定服務規劃。
- 保留已完成成果；若必須調整或重做，先說明原因、影響、保留部分與驗證方式。能透過現有功能講解、操作及小修改完成學習時，不必新增功能。
- 本專案上線使用自己的網站、資料與設定。原站的資料庫、音檔儲存、憑證與部署綁定不直接當作本專案資源。

## 交付安排與理解深度

| 時段 | 安排 |
| --- | --- |
| 平時上課 | 用老師 MD 挑選功能，按已確認教學流程實作、親自操作並確認核心理解。基礎展示與可管理服務都是累積中的成果。 |
| 期末前 | 盤點完整目標缺口、剩餘工作量及依賴；提早確認音源可播放／分析、AI 服務及部署可用性。工作量大或有阻礙的部分及早處理。 |
| 最後一週 | 可集中補齊課程未涵蓋的進階功能，完成整合、除錯、操作說明、部署與展示準備；仍依已確認範圍逐項驗證，不承諾任意剩餘量都能在一週完成。 |

- 課程核心：以教材與評量重點為準，學生能說明主要程式、親自操作並完成小修改，例如擴充搜尋條件。
- 進階功能：可由 AI 協助整合，學生需能說明用途、主要資料流程、程式來源、調整內容及如何驗證；能操作及調整重要參數，不要求自行重建所有底層演算法。
- 僅知道來源不足以代表理解。來源區分學生完成、AI 協助與原專案取用，依實際情況記錄；例如能說明「音訊 → 強弱／頻率分析 → 控制光場」，不必逐行推導繪圖演算法。
- 「AI 已檢查」「學生已操作」「理解已確認」分開記錄，沒有證據就保持待確認。進階理解深度仍須對照老師實際評分要求；若有差異，提出差異後再調整，不修改老師原文。
- 已確認的小目標內連續完成必要檢查與修正；進入新目標前依 [AGENTS.md](AGENTS.md) 的教學流程確認。文件整理不等於任何課程實作或理解驗收已完成。

## 待取得資訊與決策時機

- 收到教材時：核對週次／版本、必做題目、繳交物、期限與 AI／外部程式重用規範；保留可供新對話取得的教材原文位置或來源，缺少內容時明確標示。
- E／G 前：確認示範音檔來源、使用範圍、格式與分析可行性。
- F／I 前：確認學校或部署環境限制，再選後端、資料保存及託管方式。
- H 前：確認服務支援能力、憑證取得方式與使用預算，不在聊天或前端保存機密。
- 只有無法從現況核對、且影響方向、費用、交付或教學範圍的事項才詢問學生；一般實作選擇由代理提出具體建議。尚未回答的問題不視為已同意。
