# AI 協作紀錄

## 2026-09-21：W01 Git 初始化與署名設定

### 重要問題：為什麼 `--local` 不影響其他專案？

- 學生原先回答不會影響其他專案，理由是「本地」，並表示不知道原因。
- AI 說明：`--local` 將設定存入目前專案的 `.git/config`；`--global` 是目前使用者的共用預設值，兩者的差異是套用範圍。
- 確認方式：AI 提出「只替另一個專案設定不同署名」的情境，學生回答 `--local`。這次確認的是設定範圍的選擇。

### 採用的做法與驗證

- 學生確認先完成 Git 起步，並提供提交姓名與 email。
- AI 初始化 `main` 分支，以 `git config --local` 設定署名，並完成忽略規則與設定來源檢查；沒有修改全域 Git 設定。
- 學生親自執行 `git status` 和本專案署名查詢，貼回輸出供核對。
- 具體操作成果與概念整理見 [web.md](web.md)。

### 仍待確認

- AI 已補充「沒有 commit」與「尚未追蹤」的差別，並說明 `git add`、`git commit` 的用途；這部分尚待實作及學生回述。
- 尚未提交或推送檔案，也未代寫學生尚未表達的心得。

## 2026-09-23：核對最新教材版本

- 學生指出應按 `.gitignore` → README 的順序接續，並質疑教材 README 範例是否有 `npm run dev`；之後提供包含前兩週的 WEB03 整合版，要求更新專案指引。
- AI 以登入中的 Moodle 原文核對：較舊獨立 WEB01 的 README 範例有 `npm run dev`；最新取得的 WEB03 W01 範例只有標題、簡介、學號，首頁段落改用 7777 埠的 `http-server`。WEB03 同時包含 W02 與 W03。
- 已接受學生對教材版本和步驟順序的修正，更新指引、路線圖及進度；舊版與新版差異分開記錄。README 初稿已在工作目錄，但其建立過程和學生對內容的理解尚未回報。

## 2026-09-23：基本首頁與原專案參考界線

- 學生同意只建立名稱與簡介的首頁，AI 新增 index.html 並完成本機瀏覽器檢查；紀錄見 verification/README.md。
- 學生提醒網頁部分應盡量參考原專案。AI 核對 AGENTS.md 的取用規則與 ROADMAP B／C，確認後续頁面結構、Noir 配色及尺寸應先讀原版再逐步調整；本輪首頁只是 W01 起步，尚未取用原版畫面。
- 學生的首頁操作、HTML 理解與變更檢視仍待完成，尚未 commit。

- 學生明確確認首頁操作成功並授權首次本機提交；AI 先核對檔案與暫存差異，提交訊息採 W01 格式，未獲授權上傳 GitHub。
