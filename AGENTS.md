本專案以 [.github/copilot-instructions.md](.github/copilot-instructions.md) 為最主要指引；同一對話首次處理本專案時先讀取並遵守。已讀且內容未變更時沿用；內容變更或上下文不足時再讀取。

每次處理本專案的新訊息時，先讀取最新的 [PROGRESS.md](PROGRESS.md)，包括同一對話的後續回覆；進度維護方式見「對話進度與接續」。

同一對話首次處理本專案時，並讀取 [ROADMAP.md](ROADMAP.md)，掌握完整目標、參考版本與理解深度。收到新教材、安排實作目標或盤點期末交付時，依下方「教材對照與功能取用」處理；規劃已讀且內容未變更時可沿用。

# 放首歌：專案與代理操作指引

Copilot 指引保留使用者提供的老師英文原文。本檔補充專案導覽、工程操作、其他課程要求與已確認的教學方式；後續專案補充主要修改本檔，不改寫老師原文。若規範有實質衝突，列出差異並與學生討論。

## 專案與 codebase

- 「放首歌」是獨立、從零建立的歌曲網站；原「聽風拾曲」專案僅作參考，不在本專案的修改範圍內。
- 目前有設定、指引、規劃、進度與學習紀錄文件、已核對的 README 及基本 `index.html`；尚無 CSS、JavaScript 或 `package.json`。
- [AGENTS.md](AGENTS.md)：專案導覽、工程操作、課程補充與學習紀錄規則。
- [ROADMAP.md](ROADMAP.md)：已確認完整目標、原專案參考基準、功能地圖與課程對照、重用建議、交付安排及理解深度。
- [PROGRESS.md](PROGRESS.md)：跨對話接續用的目前進度、已確認決定、待確認事項與下一步。
- [README.md](README.md)：專案標題、目標簡介與學號欄；學生已完成修改，AI 已核對。
- [web.md](web.md)：每週網頁概念、實際成果、學生操作與理解確認紀錄。
- [ai.md](ai.md)：重要 AI 提問、建議、驗證方式與學生採用或修改的紀錄。
- [.github/copilot-instructions.md](.github/copilot-instructions.md)：老師英文原文，作為主要教學與協作指引。
- [.gitignore](.gitignore)：套件、建置產物、環境設定與日誌的排除規則。
- [index.html](index.html)：首頁包含網站名稱、簡介與兩首靜態歌曲列表；基本首頁及兩首歌曲顯示已確認，li／ol 情境選擇已答對，其餘新增標籤理解待確認。
- [verification/README.md](verification/README.md)：首頁驗證結果與可重複執行的操作步驟。
- 新增實際程式入口、模組或執行方式時，同步更新此導覽；只記錄已存在並確認的內容。

## 對話進度與接續

- 每次最終回覆前更新 `PROGRESS.md`，包含 Asia/Taipei 更新時間、本輪實際成果與目前停留點。純討論也記錄已確認決定或仍待回答的問題；沒有進度變更時如實註明，不製造完成項目。
- 維持精簡的目前狀態摘要，保留接續所需的決定、證據與待辦，不抄整段聊天。區分「AI 已檢查」「學生已操作」「理解已確認」，不把講解過的內容直接記為學生已理解。
- 寫入前再次讀取最新內容，合併其他對話已寫入的進度，只修改本輪相關部分；不得用較舊的快照覆蓋新紀錄。
- 進度文件提供背景，不取代實際檔案、Git 狀態或本次使用者指示。執行相關工作前核對現況；若與紀錄不同，修正紀錄。候選下一步仍依教學確認流程處理。
- `ROADMAP.md` 保存目標與規劃，本檔保存取用與教學流程，`PROGRESS.md` 負責即時接續；`web.md`、`ai.md` 保留既有的每週學習紀錄分工，不用每輪重寫整份心得或複製整份路線圖。

## 執行與驗證

- 本機使用 Windows PowerShell，提供符合此環境的指令。
- 基本首頁已實測可由 `npx.cmd http-server . -p 7777 -a 0.0.0.0` 啟動；驗證步驟見 `verification/README.md`。尚無建置流程或公開部署；新增指令仍須先檢查實際檔案並實測。
- 純文件修改核對內容、檔案路徑及連結；功能修改執行與變更相符的檢查，涵蓋正常操作和相關失敗情況。
- 必要檢查通過後停止技術驗證；只有新增修改、發現失敗或仍有未解問題時，才重跑或擴大相關檢查。
- 區分原始碼檢查、實際執行與瀏覽器操作的證據。回報做了哪些檢查及結果，未執行的部分如實標示。

## 代理操作規則

- 先檢查使用者的思路，直接指出錯誤前提、邏輯跳躍與資訊缺口。使用繁體中文與白話，首次出現的術語要解釋；涉及數字、人物或結論時核對來源。
- 修改前讀取相關檔案與使用位置，沿用既有寫法、原生功能或已安裝套件，採取能解決問題的最小改動。引入新工具或改變架構前先說明理由並討論。
- 除錯先重現問題，區分已確認原因與推測，在問題源頭修正，再驗證原本失敗的情況；保留有助診斷的錯誤訊息。
- 保留使用者的既有工作，修改限定在已確認範圍。重設、覆寫、刪除既有工作或改寫 Git 歷史，須有該操作的明確授權。
- API key、密碼及其他秘密不得寫入前端或提交到 Git；使用受保護的設定，`.env.example` 僅放占位文字。依功能需要檢查輸入、權限、瀏覽器安全及相依套件風險。WEB03 README 範例有學號欄；公開儲存庫前讓學生檢視個人資料與課程要求。
- Git 操作前先確認目前目錄、儲存庫與變更內容；提交前的學生檢視、格式與繳交要求依本檔「Git 與學習紀錄補充」執行。

## 教材對照與功能取用

- 目前最新的整合教材是 [WEB03 原文](https://moodle.ncnu.edu.tw/pluginfile.php/170589/mod_resource/content/0/web03.md)（[Moodle 資源頁](https://moodle.ncnu.edu.tw/mod/resource/view.php?id=91739&forceview=1)），同一份文件含 W01、W02、W03。後續收到新版時逐節核對；較早的獨立 WEB01／WEB02 只用於追溯版本差異，不把舊範例當成最新要求。
- WEB03 的 W01 順序是 `.gitignore` → `README.md` → `index.html` → 第一次 commit → GitHub。這版 README 範例只有標題、簡介與學號；較舊的獨立 WEB01 曾另外列 `npm install`、`npm run dev`。WEB03 W01 在首頁段落改用 `npx.cmd http-server . -p 7777 -a 0.0.0.0`，是有首頁後才驗證的教學指令。
- WEB03 的 W02 包含語意化 HTML、依專案需求設計 HTML／`css/`、Project Milestone 與學習紀錄；W03 以已有 demo 為前提，只有修 Web Programming 者可直接使用 7777 埠的 `http-server`。FastAPI 是另一條路徑，IIS 反向代理與 HTTPS 設定由老師負責，學生驗證本機與實際公開網址。

1. 收到老師的 MD 或作業時，先讀原文，辨認必做要求、範例、繳交格式、期限及待確認事項；指出依據的段落。保留教材原文，記錄後續對話可取得的檔案位置或來源與週次／版本；無法取得的部分標示待補，不把代理建議當成老師要求。
2. 核對實際檔案、Git 狀態與 `PROGRESS.md`，再對照 `ROADMAP.md`。分開判斷「功能已存在」與「學生已操作／理解」，不重做已完成的工作。
3. 提出精簡對照：老師要求、現有功能／檔案、可沿用內容、必要補充或衝突、最少前置、成果驗證及具體理解練習。固定題目先符合指定要求，再評估整合；課程概念列表不等於全部必做功能。
4. 從對照結果挑一個可驗證的小目標，依下一節流程進行。已有功能也可用來讀程式、操作或練習小修改，不要求每次對話新增功能；一個目標可跨多次對話。
5. 原專案取用與一般技術選擇依 `ROADMAP.md` 的界線及建議判斷；需要調整既有成果時說明原因、影響與保留部分。只詢問確實影響方向、成本、交付或教學範圍且不能自行核對的資訊。

## 已確認的教學方式

單純文件校對、既定規則整理，以及已確認目標內的必要修正，可依授權範圍直接完成與驗證，不重新啟動下列教學確認流程。

1. 有新教材時先完成上一節對照；沒有新教材時，依實際現況、已確認需求與規劃選擇候選目標。區分老師要求、已確認決定、代理建議與待確認事項。
2. 每個新的課程實作目標，先講解一小段概念，提出一個可驗證的小目標，說明做法、受影響檔案與完成標準。學生確認做法後才實作；同一範圍已確認的步驟不重複詢問。
3. 在已確認範圍內，連續完成該小步驟的實作、技術檢查與必要修正，說明改了什麼、為什麼，再給學生可以親自操作的驗證步驟與預期結果。
4. 等學生回報操作結果，再用一兩個問題或小修改確認理解，深度依 `ROADMAP.md` 的課程核心與進階功能標準。分開標示「AI 已檢查」「學生已操作」「理解已確認」；沒有實際證據的項目保持待確認，不能用程式能跑代替學生理解。
5. 根據實際操作與回答整理紀錄，再進入下一個學習步驟。只記錄實際發生的事，不代寫學生未表達的心得或宣稱未執行的測試已通過。

## Git 與學習紀錄補充

- 每次完成可驗證的小改動後，先列出修改內容，讓學生查看差異、確認理解，再進行 commit。
- 課程網頁第 7 節明定每週至少 3 次 commit，訊息採 `W<週次>: <這次做了什麼>`；週五或課程指定期限前 push 到 GitHub。以有實質內容的小改動留下紀錄，不為湊數製造修改。
- WEB03 W01 的 `0: initialize project` 是示範訊息；本專案提交訊息依上方 AI 使用規範的 `W<週次>: ...` 格式，若課堂另有指定再核對。
- 老師英文指引中的 `feat:`、`fix:` 是有意義訊息的範例；本專案依課程網頁的明定格式加上週次，例如 `W01: 建立專案首頁與使用說明`，英文原文維持不變。
- `web.md` 每週整理網頁概念、目標、實際成果、除錯與學習心得；`ai.md` 記錄重要提問、AI 建議摘要、驗證方法，以及學生採用、修改或拒絕的理由。這是本專案的紀錄分工，不必抄錄每次對話。
- 尚未完成的功能、測試或理解確認，如實標為待完成；只完成檔案或自動檢查時，不宣稱整週課程已完成。

## 補充規範的依據

- [Web 課程 AI 使用規範](https://moodle.ncnu.edu.tw/pluginfile.php/167400/mod_resource/content/0/web_AI_rules.md)：每週紀錄、Git 提交數量、格式與期限。
- [WEB03 整合教材](https://moodle.ncnu.edu.tw/pluginfile.php/170589/mod_resource/content/0/web03.md)：目前取得的最新 W01／W02／W03 內容與本機、公開部署步驟；較早的 [WEB01 獨立版](https://moodle.ncnu.edu.tw/pluginfile.php/162581/mod_resource/content/0/web01.md)僅供版本對照。
- 使用者提供的《00_WKE課程AI使用規範》簡報：AI 協作、理解責任、紀錄及評量原則。
- 本對話已確認的教學方式與文件安排：老師英文原文為主要指引，專案補充寫入本檔。
